# Orch binary + runtime state
/orch
/orch-linux
/orch-linux.gz
orch-chunk-*
notes.txt
state.db
state.db-shm
state.db-wal
# Pre-sqlite state files — left here so dev clones with an old runtime
# directory don't accidentally commit them. The runtime renames them to
# <name>.migrated on first import.
state.json
state.json.tmp
state.json.migrated
snap.json
snap.json.tmp
snap.json.bak
snap.json.migrated
snap.json.bak.migrated
/logs/
dinner.py

# Editor / scratch
.claude/
.DS_Store

# Cloudflare Wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
wrangler.jsonc

# Frontend (vite/npm)
www/node_modules/
www/dist/
www/.cache/
internal/orch/embed-dist/

# Relay (CF Worker)
cf/node_modules/
cf/.wrangler/
cf/.dev.vars
cf/.dev.vars.*
cf/dist/
# Built dashboard SPA is generated by `cd www && npm run build` and copied
# into cf/public/ at deploy time; keep cf/public/ source-controlled
# only for the landing page + favicon. Build artefacts:
cf/public/_a/
cf/public/index.html

# macOS capture build artefacts
capture/macos/.build/
capture/macos/Package.resolved
capture/ios/OrchidCapture.xcodeproj/
capture/ios/OrchidCaptureSources/
capture/ios/build/

# Misc tooling
*.swp
*.swo
node_modules/
