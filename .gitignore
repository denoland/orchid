# Orch binary + runtime state
/orch
/orch-linux
/orch-linux.gz
orch-chunk-*
notes.txt
state.json
state.json.tmp
snap.json
snap.json.tmp
/logs/
dinner.py

# Editor / scratch
.claude/
.DS_Store

# Cloudflare Wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
wrangler.jsonc

# Frontend (vite/npm)
www/node_modules/
www/dist/
www/.cache/

# Relay (CF Worker)
relay/node_modules/
relay/.wrangler/
relay/.dev.vars
relay/.dev.vars.*
relay/dist/
# Built dashboard SPA is generated by `cd www && npm run build` and copied
# into relay/public/ at deploy time; keep relay/public/ source-controlled
# only for the landing page + favicon. Build artefacts:
relay/public/_a/
relay/public/index.html

# macOS capture build artefacts
capture/macos/.build/
capture/macos/Package.resolved
capture/ios/OrchidCapture.xcodeproj/
capture/ios/OrchidCaptureSources/
capture/ios/build/

# Misc tooling
*.swp
*.swo
